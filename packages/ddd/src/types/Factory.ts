/**
 * Identifies a Factory.
 *
 * Factories encapsulate the responsibility of creating complex objects
 * in general and Aggregates in particular.
 *
 * Key characteristics:
 * - Objects created by a factory method are guaranteed to be in a valid state
 * - Encapsulates complex construction logic
 * - Can be used to enforce invariants during creation
 *
 * @see AggregateRoot
 * @see Entity
 */
export interface Factory {}
